# Solution

class Solution(object):
    def lengthOfLastWord(self, s):
        """
        :type s: str
        :rtype: int
        """
        
        x = s.split()
        len_x = len(x)-1
        word = x[len_x]
        
        return len(word)

''' 
